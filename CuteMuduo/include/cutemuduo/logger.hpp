#pragma once

#include <cutemuduo/noncopyable.hpp>

namespace cutemuduo {

enum class LogLevel { INFO, ERROR, FATAL, DEBUG };

// 单例日志类
class Logger : NonCopyable {
public:
private:
};

}  // namespace cutemuduo
